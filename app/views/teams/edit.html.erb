<%= form_for @team, html: { multipart: true } do |f| %>
  <div class="field">
    <%= f.label :schedule %>
    <%= f.file_field :schedule %>
  </div>
  <div class="actions">
    <%= f.submit 'Update' %>
  </div>
<% end %>
